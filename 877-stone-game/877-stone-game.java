class Solution {
    public boolean stoneGame(int[]arr) {
        // int alice = 0, bob = 0;
        // int i = 0;
        // int j = arr.length-1;
        // while(i<j){
        //     alice+=Math.max(arr[i], arr[j]);
        //     if (arr[i]>arr[j]) i++; 
        //     else if (arr[j]>arr[i]) j--;
        //     else
        //     bob+=Math.max(arr[i], arr[j]);
        //     if (arr[i]>arr[j]) i++; else j--;
        // }
        // return alice>bob;
        return true;
    }
}